# 🔧 端口 80 冲突完整解决方案

## 📊 问题诊断

### 第 1 步：找出是谁在占用端口 80

```bash
# 方法 1：使用 lsof
sudo lsof -i :80

# 输出示例：
# COMMAND   PID     USER   FD   TYPE DEVICE SIZE/OFF NODE NAME
# nginx     1234    root   6u   IPv4  12345      0t0  TCP *:http (LISTEN)
# apache2   5678    root   4u   IPv4  67890      0t0  TCP *:http (LISTEN)

# 方法 2：使用 netstat
sudo netstat -tlnp | grep :80

# 输出示例：
# tcp  0  0  0.0.0.0:80  0.0.0.0:*  LISTEN  1234/nginx
```

**解读输出：**
- `COMMAND`: 占用端口的程序名（nginx、apache2、httpd 等）
- `PID`: 进程ID
- `LISTEN`: 正在监听这个端口

---

## ✅ 方案 1：停止主机上的 Web 服务器（推荐用于生产）

### 如果是 Nginx

```bash
# 1. 检查 Nginx 状态
sudo systemctl status nginx

# 2. 停止 Nginx
sudo systemctl stop nginx

# 3. 禁止开机自启（重要！）
sudo systemctl disable nginx

# 4. 验证端口已释放
sudo lsof -i :80
# 应该没有输出

# 5. 启动 Docker 服务
cd /opt/flask-app
./deploy.sh start
```

### 如果是 Apache

```bash
# Ubuntu/Debian
sudo systemctl stop apache2
sudo systemctl disable apache2

# CentOS/RHEL
sudo systemctl stop httpd
sudo systemctl disable httpd

# 验证
sudo lsof -i :80
```

### 如果是其他程序

```bash
# 找到 PID (假设是 1234)
sudo lsof -i :80

# 强制终止进程
sudo kill -9 1234

# 或者更优雅的方式
sudo kill 1234
```

---

## ✅ 方案 2：修改 Nginx 容器端口（开发环境推荐）

如果你暂时不想停止主机服务，可以让 Docker Nginx 使用其他端口。

### 修改 docker-compose.yml

```yaml
services:
  nginx:
    image: nginx:alpine
    container_name: flask_nginx
    restart: always
    ports:
      - "8080:80"    # 改为 8080 端口
      - "8443:443"   # HTTPS 也改一下
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl
    depends_on:
      - web
    networks:
      - flask_network
```

### 访问方式变化

- **原来:** `http://your-server-ip`
- **现在:** `http://your-server-ip:8080`

### 防火墙配置

```bash
# 开放新端口
sudo ufw allow 8080/tcp
sudo ufw allow 8443/tcp

# 重启服务
./deploy.sh restart
```

---

## ✅ 方案 3：不使用 Nginx 容器（开发/测试推荐）

对于开发和测试环境，直接访问 Flask 即可。

### 我已经帮你注释掉了 Nginx

你的 `docker-compose.yml` 现在是这样的：

```yaml
# Nginx 反向代理（可选，用于生产环境）
# 注释掉以避免端口冲突 - 开发环境可直接访问 Flask 5000 端口
# nginx:
#   image: nginx:alpine
#   ...
```

### 直接访问 Flask

```bash
# 启动服务
./deploy.sh start

# 访问地址
http://your-server-ip:5000
```

### 防火墙配置

```bash
# 开放 5000 端口
sudo ufw allow 5000/tcp
```

---

## 🎯 推荐方案对比

| 场景 | 推荐方案 | 访问地址 | 优点 | 缺点 |
|------|---------|---------|------|------|
| **生产环境** | 方案1 (停止主机服务) | `http://ip` | 性能最好，标准端口 | 需要停止现有服务 |
| **开发环境** | 方案3 (不用Nginx) | `http://ip:5000` | 最简单，无冲突 | 缺少Nginx功能 |
| **测试环境** | 方案2 (改端口) | `http://ip:8080` | 两者共存 | 非标准端口 |

---

## 🔄 完整的启动流程（方案1）

### 步骤 1：停止主机服务

```bash
# 检查占用端口的服务
sudo lsof -i :80

# 停止并禁用（根据实际情况选择）
sudo systemctl stop nginx
sudo systemctl disable nginx

# 或
sudo systemctl stop apache2
sudo systemctl disable apache2
```

### 步骤 2：清理 Docker 容器

```bash
cd /opt/flask-app

# 停止所有容器
docker-compose down

# 清理残留容器（可选）
docker container prune -f
```

### 步骤 3：取消注释 Nginx 配置

编辑 `docker-compose.yml`，恢复 Nginx 配置：

```yaml
  # Nginx 反向代理
  nginx:
    image: nginx:alpine
    container_name: flask_nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl
    depends_on:
      - web
    networks:
      - flask_network
```

### 步骤 4：启动服务

```bash
# 启动所有容器
./deploy.sh start

# 查看状态
./deploy.sh status

# 查看日志
./deploy.sh logs nginx
```

### 步骤 5：验证访问

```bash
# 测试 Nginx
curl http://localhost

# 测试 Flask (应该被 Nginx 代理)
curl http://localhost/api/check-auth

# 在浏览器访问
http://your-server-ip
```

---

## 🔍 故障排查

### Q1: 停止了服务，端口还是被占用？

```bash
# 查看所有监听 80 端口的进程
sudo lsof -i :80

# 强制杀死进程（替换 <PID>）
sudo kill -9 <PID>

# 检查是否有残留的 Docker 容器
docker ps -a | grep nginx
docker rm -f <container_id>
```

### Q2: 启动 Nginx 容器后，访问显示 502 错误？

**原因：** Nginx 无法连接到 Flask

**解决：**
```bash
# 检查 Flask 容器是否运行
docker-compose ps

# 检查 Flask 容器日志
docker-compose logs web

# 确保 Flask 容器健康
docker-compose exec web curl http://localhost:5000
```

### Q3: 静态文件 404 错误？

**检查 Nginx 配置：**
```bash
# 查看 Nginx 配置
cat nginx/nginx.conf

# 确认静态文件路径
docker-compose exec nginx ls -la /app/static/

# 重启 Nginx
docker-compose restart nginx
```

---

## 📝 当前推荐配置

### 开发/测试环境（当前配置）

```yaml
services:
  mysql:    # 内部访问，不暴露端口
  redis:    # 内部访问，不暴露端口
  web:      # 暴露 5000 端口
    ports:
      - "5000:5000"
  # nginx:  # 已注释，避免端口冲突
```

**访问:** `http://your-server-ip:5000`

### 生产环境（推荐配置）

```yaml
services:
  mysql:    # 内部访问
  redis:    # 内部访问
  web:      # 内部访问，不暴露端口
    # ports:
    #   - "5000:5000"  # 注释掉，仅通过 Nginx 访问
  nginx:    # 对外唯一入口
    ports:
      - "80:80"
      - "443:443"
```

**访问:** `http://your-server-ip`

---

## 💡 最佳实践

### 1. 生产环境必须使用 Nginx

✅ **使用 Nginx 的原因：**
- 性能：处理静态文件快 10 倍
- 安全：隐藏 Flask 端口，防止直接攻击
- 灵活：支持负载均衡、限流、HTTPS
- 稳定：Nginx 比 Flask 开发服务器稳定得多

### 2. 端口管理策略

```
生产环境开放端口：
✅ 80  (Nginx HTTP)
✅ 443 (Nginx HTTPS)
❌ 5000 (Flask - 不对外暴露)
❌ 3306 (MySQL - 不对外暴露)
❌ 6379 (Redis - 不对外暴露)
```

### 3. 服务器上只运行一个 Web 服务器

❌ **错误做法：**
- 主机运行 Nginx
- Docker 也运行 Nginx
- 两者争抢 80 端口

✅ **正确做法：**
- 选择一个：要么主机 Nginx，要么 Docker Nginx
- 主机 Nginx 适合管理多个项目
- Docker Nginx 适合项目独立部署

---

## 🚀 快速决策树

```
你需要 Nginx 吗？
├─ 是 (生产环境)
│  ├─ 主机已有 Nginx？
│  │  ├─ 是 → 用主机 Nginx 反向代理到 :5000
│  │  └─ 否 → 停止主机服务，用 Docker Nginx
│  └─ 执行方案 1
│
└─ 否 (开发/测试)
   └─ 直接访问 Flask :5000
      └─ 当前配置已满足
```

---

## 📞 需要帮助？

根据你的情况，选择一个方案执行：

**如果你想用标准 80 端口访问（生产环境）：**
```bash
sudo systemctl stop nginx
sudo systemctl disable nginx
# 然后取消注释 docker-compose.yml 中的 nginx 部分
./deploy.sh restart
```

**如果你只是开发测试（当前最简单）：**
```bash
# 什么都不用改，直接访问
http://your-server-ip:5000
```

告诉我你想用哪种方案，我可以提供具体的操作步骤！
