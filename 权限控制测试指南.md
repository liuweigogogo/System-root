# 用户管理权限控制 - 测试指南

## 📊 **数据库权限字段信息**

### 字段详情：
- **字段名**: `role`
- **数据类型**: `ENUM('user', 'admin')`
- **表名**: `users`

### 权限值：
| 角色 | 值 | 说明 |
|------|---------|------|
| 管理员 | `'admin'` | 拥有所有权限，可以管理所有用户 |
| 普通用户 | `'user'` | 只能查看自己的信息，无增删改权限 |

---

## 🔍 **如何验证用户角色**

### 方法1: 直接查询数据库

```sql
-- 查看所有用户及其角色
SELECT id, username, email, role, is_active, created_at 
FROM users 
ORDER BY created_at DESC;

-- 只查看管理员
SELECT id, username, email, role 
FROM users 
WHERE role = 'admin';

-- 只查看普通用户
SELECT id, username, email, role 
FROM users 
WHERE role = 'user';
```

### 方法2: 修改用户角色

```sql
-- 设置某个用户为管理员
UPDATE users SET role = 'admin' WHERE username = 'admin';

-- 设置某个用户为普通用户
UPDATE users SET role = 'user' WHERE username = 'zhangsan';

-- 设置用户ID为1002的用户为管理员
UPDATE users SET role = 'admin' WHERE id = 1002;
```

---

## ✅ **权限控制功能清单**

### 管理员 (role = 'admin')：
- ✅ **能看到** "用户管理"菜单项
- ✅ **能访问** 用户管理页面
- ✅ **能查看** 所有用户列表
- ✅ **能使用** 搜索和过滤功能
- ✅ **能点击** "添加用户"按钮
- ✅ **能点击** "编辑"按钮
- ✅ **能点击** "删除"按钮
- ✅ **能查看** 完整的统计信息
- ✅ **能使用** 分页功能

### 普通用户 (role = 'user')：
- ❌ **看不到** "用户管理"菜单项（被隐藏）
- ❌ **如果强制访问**，会收到403错误或只看到自己的信息
- ✅ **只能看到** 自己的用户信息
- ❌ **没有** "添加用户"按钮
- ❌ **没有** "编辑"按钮
- ❌ **没有** "删除"按钮
- ❌ **没有** 搜索和过滤功能
- ❌ **没有** 分页功能
- ✅ **看到提示**："你是普通用户，只能查看自己的信息，无法管理其他用户。"

---

## 🧪 **测试步骤**

### 测试场景1: 管理员登录测试

#### 步骤：
1. 确保数据库中admin用户的role为'admin'：
   ```sql
   UPDATE users SET role = 'admin' WHERE username = 'admin';
   ```

2. 访问登录页: http://127.0.0.1:5000/login

3. 使用管理员账户登录：
   - 用户名: `admin`
   - 密码: `admin123`

4. 登录后检查：
   - ✅ 左侧菜单应该显示"系统设置" → "用户管理"
   - ✅ 点击"用户管理"应该能打开
   - ✅ 应该能看到所有用户列表
   - ✅ 应该能看到"添加用户"按钮
   - ✅ 每个用户后面应该有"编辑"和"删除"按钮

#### 预期结果：
```
✅ 所有功能正常显示
✅ 能管理所有用户
✅ 统计数据显示真实数据
```

---

### 测试场景2: 普通用户登录测试

#### 步骤：
1. 首先创建一个普通用户（如果还没有）：
   ```sql
   -- 方法1: 通过管理员界面添加
   -- 方法2: 直接SQL插入
   INSERT INTO users (username, password_hash, email, role) 
   VALUES ('testuser', '$2b$12$...', 'test@example.com', 'user');
   
   -- 或者修改现有用户为普通用户
   UPDATE users SET role = 'user' WHERE username = 'zhangsan';
   ```

2. 退出当前登录（如果已登录）

3. 使用普通用户账户登录：
   - 用户名: `testuser` (或 `zhangsan`)
   - 密码: 对应的密码

4. 登录后检查：
   - ❌ 左侧菜单"系统设置"下应该**没有**"用户管理"选项
   - ✅ 如果直接访问URL也应该被拦截

#### 预期结果：
```
❌ "用户管理"菜单项完全隐藏
❌ 即使知道URL也无法访问（被前端和后端双重拦截）
✅ 可以正常访问其他功能（文件转换等）
```

---

### 测试场景3: 普通用户强制访问测试

#### 步骤：
1. 以普通用户身份登录

2. 尝试直接访问用户管理API：
   ```javascript
   // 在浏览器控制台执行
   fetch('/api/users', {credentials: 'include'})
     .then(r => r.json())
     .then(console.log)
   ```

#### 预期结果：
```json
{
  "success": false,
  "message": "权限不足，仅限管理员访问",
  "error_code": "PERMISSION_DENIED"
}
```

HTTP状态码: **403 Forbidden**

---

## 🔒 **权限控制的多层防护**

### 第1层: 前端UI隐藏
- 位置: `dashboard.js` - `updateUIBasedOnRole()`
- 作用: 普通用户看不到"用户管理"菜单项

### 第2层: 前端功能限制
- 位置: `user_management.js` - `hideAdminFeatures()`
- 作用: 即使进入页面，也隐藏增删改按钮

### 第3层: 后端权限验证
- 位置: `PermissionService.py` - `@admin_required`装饰器
- 作用: API级别的权限检查，返回403错误

### 第4层: 数据库级别
- 位置: `users`表的`role`字段
- 作用: 权限的最终判断依据

---

## 📝 **常见问题排查**

### Q1: 管理员也看不到"用户管理"菜单？

**检查清单：**
```sql
-- 1. 检查数据库中role字段值
SELECT username, role FROM users WHERE username = 'admin';

-- 2. 如果role不是'admin'，更新它
UPDATE users SET role = 'admin' WHERE username = 'admin';
```

**前端检查：**
```javascript
// 在浏览器控制台执行
console.log(window.currentUser);
// 应该看到: {id: 1, username: "admin", role: "admin", ...}
```

### Q2: 普通用户还能看到"用户管理"菜单？

**原因：** 可能是缓存问题或页面未刷新

**解决方法：**
1. 硬刷新页面 (Ctrl + F5)
2. 清除浏览器缓存
3. 检查`window.currentUser.role`的值

### Q3: 所有用户都看不到"用户管理"？

**检查：**
```javascript
// 浏览器控制台
console.log(window.currentUser);
```

如果显示`undefined`，说明用户信息加载失败。

**解决：**
1. 检查`/api/users/{userId}`接口是否正常
2. 检查Network标签看请求是否成功
3. 检查是否有JavaScript错误

---

## 🎯 **快速验证命令**

### 设置admin为管理员：
```sql
UPDATE users SET role = 'admin' WHERE username = 'admin';
```

### 设置zhangsan为普通用户：
```sql
UPDATE users SET role = 'user' WHERE username = 'zhangsan';
```

### 查看所有用户角色：
```sql
SELECT id, username, role FROM users;
```

### 创建一个新的管理员用户：
```sql
INSERT INTO users (username, password_hash, email, role) 
VALUES ('admin2', '$2b$12$...hash...', 'admin2@example.com', 'admin');
```

---

## 📞 **测试结果报告模板**

### 测试环境：
- 浏览器: Chrome/Firefox/Edge
- 服务器状态: 运行中
- 数据库: MySQL

### 测试结果：

#### 管理员测试 (username: admin)
- [ ] 能看到"用户管理"菜单
- [ ] 能访问用户管理页面
- [ ] 能看到所有用户
- [ ] 有"添加用户"按钮
- [ ] 有"编辑"和"删除"按钮

#### 普通用户测试 (username: testuser)
- [ ] 看不到"用户管理"菜单
- [ ] 无法访问用户管理页面
- [ ] API返回403错误

#### 数据库验证
```sql
-- 执行结果：
SELECT username, role FROM users;

+----------+-------+
| username | role  |
+----------+-------+
| admin    | admin |
| zhangsan | user  |
| lisi     | user  |
+----------+-------+
```

---

## ✅ **修复总结**

### 已实现的权限控制：

1. **前端菜单隐藏** - 普通用户看不到"用户管理"
2. **前端功能限制** - 普通用户只能看自己，无增删改按钮
3. **后端权限验证** - API级别的@admin_required装饰器
4. **数据隔离** - 普通用户只能查看自己的信息

### 现在的用户体验：

**管理员：**
- 完整的用户管理功能
- 可以管理所有用户
- 所有操作权限

**普通用户：**
- 完全看不到"用户管理"菜单项
- 即使强制访问也会被拦截
- 只能使用其他非管理功能

---

**现在可以测试了！请按照上面的步骤验证权限控制是否正常工作。** 🎉
